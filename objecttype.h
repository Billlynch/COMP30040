#ifndef OBJECTTYPE_H
#define OBJECTTYPE_H
/*!
 * \brief The ViewType enum
 * The enum used to identify the object and what to focus on in the 3D view.
 */
enum ObjectType { centre, sample, pem, polarFilter, analyser, laser, notAnObject };

#endif // OBJECTTYPE_H
